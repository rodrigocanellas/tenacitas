SUBDIRS = \
  concurrent/concurrent_lib \
  logger/logger_lib \
  logger/logger_tst


QT -= core
QT -= widgets
QT -= network
QT -= gui

TEMPLATE = subdirs

# build the project sequentially as listed in SUBDIRS !
CONFIG += ordered

