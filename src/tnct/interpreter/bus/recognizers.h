
#ifndef TNCT_INTERPRETER_BUS_RECOGNIZERS_H
#define TNCT_INTERPRETER_BUS_RECOGNIZERS_H

#include <vector>

#include "tnct/interpreter/bus/recognizer.h"

namespace tnct::interpreter::bus {

using recognizers = std::vector<bus::recognizer>;

} // namespace tnct::interpreter::bus
#endif
