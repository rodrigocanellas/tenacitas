#include <measures/time/weekday.h>

namespace tenacitas {
namespace measures {
namespace time {

const weekday weekday::sun(1);
const weekday weekday::mon(2);
const weekday weekday::tue(3);
const weekday weekday::wed(4);
const weekday weekday::thu(5);
const weekday weekday::fri(6);
const weekday weekday::sat(7);

} // namespace time
} // namespace measures
} // namespace tenacitas
